# node-modules rather than Yarn 4's default Plug'n'Play. PnP would need
# verifying against this project's less standard tooling - the jison parser
# generator installed from a git fork, and typedoc - and the migration's goal
# was getting off Yarn 1, not changing how modules resolve. Worth revisiting
# as its own change.
nodeLinker: node-modules
